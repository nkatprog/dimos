# Nix build output symlinks
result
result-*

# Build lock used by ArduinoModule._build_bridge()
.bridge_build.lock

# Arduino sketch build artifacts (also hosts the generated dimos_arduino.h)
examples/*/sketch/build/
